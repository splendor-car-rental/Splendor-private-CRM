rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // SPLENDOR PRIVATE CRM MASTER RULES
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
